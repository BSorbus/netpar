<div class="row">
  <div class="col-sm-12">
    <blockquote>
      <p>
      Akceptując zgłoszenie musisz powiązać osobę opisaną w elektronicznym zgłoszeniu z już istniejącym w systemie Netpar2015 Klientem (lub dodać nowego klienta i dopiero wtedy połączyć)<br>
      ...Wyszukaj Klienta, zaznacz w tabeli poniżej (poprzez "klik") wybrany w wiersz i naciśnij "Aktualizuj Elektroniczne Zgłoszenie".<br>
      (Jeżeli nie zaznaczysz wiersza w tabeli przycisk "Aktualizuj Elektroniczne Zgłoszenie" będzie zablokowany)
      </p>
    </blockquote>
  </div> <!-- /col-sm-6 -->
</div> <!-- /row -->

<% search_value = f.object.pesel.blank? ? "#{f.object.name} #{f.object.given_names}" : "#{f.object.pesel}" %>
<%= f.hidden_field :customer_id %>

<div class="row">
  <div class="col-sm-12">

    <table id="customers-for-select" class="hover cell-border" width="100%" 
      data-source="<%= datatables_for_select_index_customers_path(format: :json) %>"
      data-initialsearch="<%= search_value %>">
      <thead>
        <tr>
          <th> 0 ID </th>
          <th> 1 Email</th>
          <th> 2 Name</th>
          <th> 3 Given names</th>
          <th> 4 Pesel</th>
          <th> 5 Birth Place</th>
          <th> 6 Birth Date</th>
          <th> 7 Family name</th>
          <th> 8 Address postal code</th>
          <th> 9 Address city</th>
          <th>10 Address street</th>
          <th>11 Address house</th>
          <th>12 Address number</th>
        </tr>
      </thead>

      <tbody>
         <!--data from JSON -->
      </tbody>
    </table>


  </div> <!-- /col-sm-12 -->

  <div class="col-sm-12">

    <% if policy(:customer).new? %>

      <%= link_to new_customer_path(customer: { name: f.object.name, 
                                                given_names: f.object.given_names,
                                                pesel: f.object.pesel,
                                                birth_date: f.object.birth_date,
                                                birth_place: f.object.birth_place,
                                                phone: f.object.phone,
                                                email: f.object.email,
                                                family_name: f.object.family_name,
                                                citizenship_code: f.object.citizenship_code,

                                                lives_in_poland: f.object.lives_in_poland,
                                                address_id: f.object.address_id,
                                                teryt_code: f.object.teryt_code,
                                                province_code: f.object.province_code,
                                                province_name: f.object.province_name,
                                                district_code: f.object.district_code,
                                                district_name: f.object.district_name,
                                                commune_code: f.object.commune_code,
                                                commune_name: f.object.commune_name,
                                                city_code: f.object.city_code,
                                                city_name: f.object.city_name,
                                                city_parent_code: f.object.city_parent_code,
                                                city_parent_name: f.object.city_parent_name,
                                                street_code: f.object.street_code,
                                                street_name: f.object.street_name,
                                                street_attribute: f.object.street_attribute, 

                                                c_address_city: f.object.city_name,
                                                c_address_street: f.object.street_name,
                                                c_address_house: f.object.c_address_house,
                                                c_address_number: f.object.c_address_number,
                                                c_address_postal_code: f.object.c_address_postal_code,
                                                address_city: f.object.city_name,
                                                address_street: f.object.street_name,
                                                address_house: f.object.c_address_house,
                                                address_number: f.object.c_address_number,
                                                address_postal_code: f.object.c_address_postal_code
                                              }, 
            back_url: edit_approved_proposal_path(@proposal.category.downcase, @proposal) ), class: "btn btn-primary" do %>
        <span class="fa fa-plus"></span>
        <%= t("helpers.links.new") %>
      <% end %>

    <% end  %> 
  </div> <!-- /col-sm-12 -->

</div> <!-- /row -->


